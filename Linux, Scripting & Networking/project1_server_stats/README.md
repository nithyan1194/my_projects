create a lightweight script that can be executed on any Linux server to instantly output critical system health metrics.

Requirements: Capture OS version, total/used CPU, memory usage, disk utilization, and the top 5 processes consuming resources.

Constraint: It must rely only on native Linux tools—no installing heavy third-party monitoring agents (like Datadog or New Relic) yet.
